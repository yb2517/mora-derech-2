# CLAUDE.md: מסמך הבנייה של מורה הדרך 2.0

סטטוס: גרסה 2.3, מאושרת 14.09.2026. הקובץ יושב בשורש המאגר בשם CLAUDE.md, ועותק ב-project-docs בשם doc-build-document.md.
תאריך: 13.09.2026
צעד 5 בתוכנית בנייה 03 גרסה 2. נכתב לפי מתודולוגיית הכוכב (פריט 45) פרק ח.
**כלל העל של המסמך הזה**: הוא מתרגם את מפת המודולים לקוד ואינו מוסיף עליה. כל כלל כאן מפנה לסעיף במפה. סתירה בין המסמך הזה למפה: המפה גוברת, והסתירה היא פער שמדווח ואינו מתוקן בקוד.

## 1. המקורות המחייבים

| המסמך | מה הוא קובע | מעמד |
|---|---|---|
| doc-module-map.md (גרסה 3, מאושרת 12.09.2026) | הישויות, החוקים העסקיים, המודולים, החוזה, רשימת המותר, קודי השגיאה, מערך הבדיקות, סדר הבנייה | מסמך הייחוס. משתנה לפני הקוד, לעולם לא אחריו |
| תשעת מקרי השימוש (project-docs) | הזרימות, כל צעד עם סוג רכיב ודרגת מימוש | המקור לבדיקות ה-System |
| prd-morei-haderech.md (גרסה 1.4) | הדרישות הממוספרות וקריטריוני הקבלה | המקור לבדיקות ה-Unit |
| decision-01, decision-02, decision-03, decision-04 | הישויות, שרשרת הזכויות, הבסיס הטכני, יעד ההרצה וסביבת הבנייה | מחייבות |
| doc-decision-round-01.md | ההכרעות שנקלטו במפה גרסה 3 | מחייב |
| שלושת מסמכי הבן (אוטומציה, סוכנים, ממשקים) | פירוט לפי סוג רכיב | מחייבים |
| course-methodology-star-architecture (פריט 45) | המתודולוגיה | מחייבת, לקריאה בלבד |

הסוכן שמוצא טעות באחד מאלה מדווח פער ואינו עורך. עדכון מסמך הוא פעולה של בעלת הפרויקט בלבד, בגרסה חדשה.

## 2. מה כבר קיים ואין לבנות מחדש

| מה | היכן | מה עושים איתו |
|---|---|---|
| prototype-mora-darech-jaffa-v1.html (בנייה 02, 84KB) | project-docs | **אינו נקודת המוצא של הקוד.** הוא מפרט התנהגות ומקור לקורפוס. הקוד נכתב מהמפה, ולוקח ממנו: 19 פריטי הקורפוס עם הטקסט, התחנה, העמוד והקואורדינטות; שלוש התנהגויות הקול שנבדקו (בחירת קול עברי מפורשת, שרשור קטעים, השהיה אחרי ביטול); מנוע הדירוג הלקסיקלי הדו שלבי; סימולטור ההליכה |
| 19 פריטי הקורפוס | בתוך הקובץ | מועברים לרשומות בשלב 7. עד אז נתוני הדגמה |
| פעולת "אפס למצב ההתחלה" | בתוך הקובץ | **אינה מועברת.** הוכרע 10.09: יוצאת מהמערכת |
| ערכי הסף (40, 20, 0.28, 60) | בבלוק thresholds בקוד | מועברים לטבלת ה-reference בשלב 1. לא ייכתבו בקוד |

## 3. הערימה הטכנית ומשתני הסביבה

| הפריט | הערך |
|---|---|
| יעד ההרצה, שלבים 1 עד 4 | קובץ יחיד בלחיצה כפולה (decision-04 ב1) |
| יעד ההרצה, שלבים 5 עד 7 | ענן (decision-04 ב4). הספק [טרם נקבע] |
| השפה | JavaScript [הצעה, נגזרת מהאב טיפוס שרץ בדפדפן; אף מסמך מאושר לא הכריע שפה]. אין תהליך בנייה ואין תלות חיצונית בשלבים 1 עד 4 |
| דרייבר ה-Repository, שלבים 1 עד 4 | אחסון הדפדפן |
| דרייבר ה-Repository, שלבים 5 עד 7 | מסד ענן [טרם נקבע] |
| מאגר, קוד ותוצרי קוד | Git. ענף לכל שלב |
| מאגר, מסמכי הפרויקט והדוחות | דרייב, תיקיית drafts, לפי הסקילים |

משתני הסביבה, בשמות בלבד. ערכים לעולם לא במסמך, בקוד או בצ'ט: DB_URL, DB_KEY, AI_PROVIDER_KEY (לא בשימוש ב-v1). [השמות הם הצעה; ישונו לפי המוסכמה של ספק הענן שייבחר, הכרעה פתוחה 2 במפה]

## 4. חוקי הברזל של המאגר

1. כל בקשה נוסעת בשתי המעטפות של CORE-01. אף מודול אינו מגדיר מעטפה משלו.
2. אף מודול אינו קורא למודול אחר ישירות. כל פנייה עוברת ב-CORE-02 מול רשימת המותר.
3. רק CORE-04 נוגע באחסון. חיפוש מחרוזת החיבור במאגר מחזיר קובץ אחד.
4. כותב אחד לכל עמודה, לפי BL-09 במפה.
5. כל ערך משתנה נקרא מטבלת ה-reference. ערך חסר מחזיר E-REF-EMPTY ואינו מומצא.
6. סודות בכספת או במשתני סביבה בלבד.
7. כיוון תלות אחד: מסך, endpoint, Orchestrator, מודול, Repository, אחסון. אין ייבוא בין מודולים.
8. קוד שגיאה שאינו ברשימה הסגורה של המפה אינו נזרק.
9. בדיקה נכתבת יחד עם הקוד, לא אחריו, ונגזרת ממקור: Unit מקריטריוני הקבלה, ממשקים מרשימת המותר ומקודי השגיאה, System ממקרי השימוש. בדיקה בלי מקור היא המצאה.

## 5. מיפוי המודולים לקבצים

[עץ הקבצים הזה הוא הצעה שלי ולא נגזרת ממסמך מאושר: המפה קובעת מודולים, לא תיקיות. הוא אושר כהצעה ב-13.09.2026 יחד עם גרסה 2, ומחייב מרגע זה. שינוי בו הוא פער שמדווח, לא סטייה שקטה.]

```
/index.html               נקודת הכניסה: קוראת את הנתונים, בונה דרייבר, Repository ו-Orchestrator, ומגישה את המסכים
/core/contract.js         CORE-01: שתי המעטפות, validate, רשימת הקודים
/core/errors.js           CORE-01: הקודים וההסבר למפתח
/core/orchestrator.js     CORE-02: חמשת הצעדים
/core/business-logic.js   BL-01 עד BL-21, טבלת המעברים, תנאי הנעילה L1 עד L6
/registry/modules.json    CORE-03
/registry/allow-list.json CORE-03
/repository/index.js      CORE-04: הפעולות בשם עסקי
/repository/driver-browser.js   הדרייבר של שלבים 1 עד 4
/repository/driver-cloud.js     הדרייבר של שלבים 5 עד 7
/services/governance.js   BE-05
/services/gate.js         BE-06
/services/retrieval.js    BE-04
/services/retrieval-ranker.js   מנוע הדירוג, מודול נשלף (הכרעה ד1)
/services/dialogue.js     BE-03
/services/delivery.js     FE-04
/services/log.js          BE-07
/screens/veto/            FE-06
/screens/traveler/        FE-05
/screens/admin/           FE-07 ו-FE-08, מסך אחד עם role (הכרעה ג3)
/design/tokens.css        DESIGN-01
/design/components/       DESIGN-01
/connectors/stt.js        CONN-01
/connectors/tts.js        CONN-02
/connectors/location.js   CONN-03
/automation/geofence.js   AUTO-01
/automation/timer.js      AUTO-02
/gateways/ai.js           GW-01, תפר ריק
/tools/simulator.js       TOOL-01, פיתוח בלבד
/data/reference.json      טבלת ה-reference
/data/demo/               נתוני הדגמה מסומנים
/tests/                   לפי רמות: unit, interfaces, system, structure
/tests/helpers/           עזרי בדיקה ומודול הדמה
/tests/run-all.js         המריץ האחד של המערך
```

## 6. סדר הבנייה ובדיקת הקבלה של כל שלב

מהמפה סעיף 7. לכל שלב: ענף נפרד, משימה אחת בכל פעם, דוח שלב, תיקון מסמכים לפני קוד נוסף, מיזוג אחרי בדיקות ירוקות, שיחה חדשה לשלב הבא.

| שלב | מה נבנה | בדיקת הקבלה: מה עושים ומה חייבים לראות |
|---|---|---|
| 1 | CORE-01 עד CORE-04, שלוש טבלאות המערכת, טבלת ה-reference, דרייבר הדפדפן | שולחים בקשה מותרת: עוברת, ונרשמות שתי שורות audit_log עם אותו request_id. שולחים approve מ-screen-traveler: נדחית ב-E-ALLOW-DENIED ונרשמת. מחפשים את מחרוזת החיבור: קובץ אחד. מצליבים את קודי השגיאה בקוד מול המפה: זהים |
| 2 | DESIGN-01, ואז ארבעת המסכים על נתוני הדגמה | כל מסך שולח מעטפות בלבד ואינו קורא ל-Repository. מחליפים ערכת עיצוב: אף מסך לא משתנה. מוסיפים מסך דמה: שורה ברשימת המותר מספיקה |
| 3 | Slice ראשון: F-07 מקצה לקצה, כולל register_mou ו-M-06 מרשומה | החוקר מאשר פריט, ורואה רשומת APPROVALS עם זמן, מבצע, מצב קודם וחדש. מכשילים את כתיבת ה-APPROVALS: מצב הפריט אינו משתנה (BL-01). רושמים הסכם: M-06 עובר מ-0 ל-1 ומחושב מהרשומה, והמסך מציג את מצב השער ואת מה שחסר לפתיחתו. השער עצמו נפתח רק כשגם המסלול נעול (BL-08), והנעילה היא שלב 4 |
| 4 | BE-05 מלא, BE-06, BE-04, BE-03, FE-04, BE-07 עם חישוב | שלוש הבדיקות האדומות (סעיף 8) נכשלות כמצופה. שואלים על פריט pending: הימנעות, והפריט אינו מוזכר. שתי הגעות בהפרש 5 שניות: השנייה מוחזקת 15 |
| 5 | CONN-01 עד CONN-03, AUTO-01, AUTO-02, TOOL-01 | הליכה מלאה בסימולטור, 12 תחנות: 12 arrive, כל שורה עם דגל simulator. דגימה בדיוק 80 מטר: אין אירוע |
| 6 | GW-01 כתפר ריק | חיפוש קריאה לספק AI: קובץ אחד. קריאה עם model_tier ריק: E-REF-EMPTY, בלי פנייה החוצה |
| 7 | ניקוי נתוני הדגמה, הקורפוס לרשומות, אימות עוגנים בשטח | L1 עד L4 עוברים על 19 הפריטים; נעילה ראשונה מצליחה ונרשמת |

## 7. נתוני הדגמה

יושבים ב-/data/demo/ ומסומנים בשדה is_demo. 19 פריטי הקורפוס, מכון אחד, הסכם אחד, ושלושה סשנים מלאכותיים עם 2, 3 ו-5 שאלות (לבדיקת M-01 = 3). [המספרים הם דוגמה ולא חובה; מה שמחייב הוא שנתוני הדגמה מסומנים ומוסרים בשלב 7] הסרה בשלב 7, ובדיקת קבלה: שאילתה על is_demo מחזירה אפס.

## 8. הבדיקות האדומות

שלוש, נכתבות כדי להיכשל אם ההגנה תוסר. כשל אמיתי בהן חוסם שחרור:
1. פריט pending מוזרק למאגר המועמדים, והשליפה מחזירה אותו (F-05, K1).
2. פריט approved בלי רשומת APPROVALS עובר את L1 (F-08).
3. שורת initiated שנשלחה שלא ממודול השיחה נספרת ב-M-01 (F-09).

## 9. מה עוצר את הבנייה, תמיד

לעצור, לרשום, לדווח. אין השלמה עצמית. [הרשימה היא ריכוז מרוח המפה, המתודולוגיה ומנגנון הבנייה, ולא ציטוט ממקום אחד]

1. צורך לשנות את משמעות הליבה, הישויות או המעטפה: עולה לבעלת הפרויקט, אינו מתוקן בקוד.
2. תיקון או שיפור בקוד הליבה: Commit נפרד ומסומן, עם Regression מלאה.
3. שינוי Schema של נתונים פרטיים: הצעה מאושרת קודם.
4. ערך חסר בטבלת ה-reference שהמערכת אינה יכולה לתפקד בלעדיו.
5. דרישה בלי מקור, או מודול בלי שורה במפה.
6. קוד שגיאה שאינו ברשימה הסגורה.
7. שער פתוח לפי סעיף 8 במפה.
8. פער בין המסמך למאגר בפועל.

## 10. מבחן הקבלה של המערכת כולה

שמונת מבחני המבנה, בסוף כל שלב: חיבור לנתונים בקובץ אחד; קריאה לספק AI בקובץ אחד; כל מסך פונה לכתובת אחת ומצהיר מי הוא; כל בקשה מותירה שורות audit_log עם מזהה משותף; ערכים משתנים מטבלת ה-reference; הוספת מסך היא שורה ברשימת המותר; קודי השגיאה זהים בקוד ובמפה; גרף הייבוא מראה כיוון תלות אחד.

## 11. גבולות הסוכן

הסוכן רשאי: לכתוב קוד לפי המפה, ליצור ענף, להריץ בדיקות, לתקן עד שהן עוברות, לכתוב דוח שלב, ולמזג אחרי בדיקות ירוקות ואימות מול המאגר.

הסוכן אינו רשאי: לערוך את המפה או את המסמך הזה; להמציא מודול, פעולה, קוד שגיאה או שדה; לכתוב ערך משתנה בקוד; לגעת בליבה בלי הכללים של סעיף 9; לכתוב סוד; למחוק נתונים; למזג בלי בדיקות; להרחיב את היקף השלב; **לבנות מודולי שירות (BE-03 עד BE-07, FE-04 המלא) לפני שהמסכים על נתוני הדגמה אושרו במפורש בידי בעלת הפרויקט** (build-mechanism, נקודות האישור: המסכים לפני המודולים).

ארבעת הדברים שרק אדם מאשר, לפי מנגנון הבנייה: מפת המודולים וגרסאותיה; המסכים לפני המודולים; שינוי ליבה או Schema; הפריסה לייצור. כל השאר מבוצע ומדווח בשורה.

## 12. נספח: שלושת הפרומפטים של מנגנון הבנייה

המקור: build-mechanism.md (פריט 46 בספריית הקורס, גרסה 1.1, 07.09.2026). מועתקים כלשונם מפני שספריית הקורס אינה זמינה לסוכן במאגר. סתירה בין הנספח למקור: המקור גובר, והנספח מתעדכן.

### פרומפט 2: תוכנית השלב (רץ בפתיחת כל שלב)

תפקיד: אתה בונה את שלב [מספר ושם השלב] לפי מפת המודולים ומסמך הבנייה של הפרויקט.

חוק ברזל: כל פריט בתוכנית נגזר מדרישה ממוספרת או משורה במפה. פריט בלי מקור אינו נכנס. אין להרחיב את היקף השלב.

החזר, לאישור לפני כל כתיבה: רשימת המשימות בסדר ביצוע, ולכל משימה: הדרישה, המודול, הקבצים שייגעו בהם, ובדיקת הקבלה שלה בניסוח מה עושים ומה חייבים לראות; רשימת השערים וההכרעות הפתוחות שהשלב תלוי בהן; ומה במפורש מחוץ להיקף השלב.

### פרומפט 3: דוח השלב ובדיקת הקבלה (רץ בסגירת כל שלב)

בדוק את מה שנבנה זה עתה מול המפה ומסמך הבנייה, ואל תסתמך על זיכרון השיחה: אמת מול המאגר, המסד או התיקיות בפועל.

לפני הדוח, הרץ את מערך הבדיקות המלא לפי פרק ז במתודולוגיה: Unit Test לכל מודול שנגעת בו, בדיקות הממשקים (המעטפה, רשימת המותר, קודי השגיאה), ו-System Test על התרחישים שהשלב משלים, כ-Regression על כל מה שכבר נבנה. מסירה בלי מערך ירוק אינה מסירה. כשקיימת רשת סוכני בדיקה, היא רצה עצמאית והדוח שלה מצורף.

החזר: טבלת המשימות עם תוצאת בדיקת הקבלה של כל אחת; תוצאות מערך הבדיקות לפי רמות; תוצאות המבחנים המבניים שכבר ניתנים להרצה (חיבור למסד בקובץ אחד, פנייה לספק ה-AI בקובץ אחד, כיוון תלות אחד, קודי שגיאה זהים למפה); רשימת הקבצים והשינויים; משימות תחזוקה ו-Technical Debt שנרשמו; וטבלת הפערים: כל מקום שבו הקוד גילה שהמפה או מסמך הבנייה חסרים או סותרים, עם ההפניה המדויקת. דוח בלי פערים סוגר את השלב; דוח עם פערים מפעיל את פרומפט 4 לפני כל קוד נוסף.

### פרומפט 4: תיקון המסמכים מפערי הקוד (רץ כשדוח מחזיר פערים)

תפקיד: אתה מעדכן את מפת המודולים או את מסמך הבנייה לפי טבלת הפערים מדוח השלב.

חוק ברזל: תקן רק את מה שעולה מהפערים. אין להוסיף מודולים, פעולות או יכולות שאיש לא ביקש. פער שמערער על הליבה או על גבולות ה-MLP אינו מתוקן כאן: הוא חוזר לבעלת הפרויקט כהחלטת אפיון.

בצע: לכל פער, ההכרעה המוצעת והסעיף שמשתנה; גרסה חדשה של המסמך המתוקן, עם שורת יומן; והצהרה מה לא השתנה. אחרי אישור הגרסה: העתקה ל-prd-update-inbox, והקוד ממשיך לפי המסמך המעודכן.

### מחזור השלב, שישה צעדים

תוכנית השלב (פרומפט 2) מוצגת ומאושרת; הבנייה, משימה אחת בכל פעם; דוח השלב (פרומפט 3); תיקון המסמכים (פרומפט 4) רק כשיש פערים; סגירה עם אימות בפועל; שיחה חדשה לשלב הבא. ההקשר חי במפה ובמסמך הזה, ולא בזיכרון השיחה.

## 13. יומן גרסאות

גרסה 2.3, 14.09.2026: פער 34 שדוח שלב 3 פתח, בהכרעת בעלת הפרויקט. סעיף 6: בדיקת הקבלה של שלב 3 נוסחה מחדש כך שתתיישב עם BL-08 במפה: רישום הסכם מביא את M-06 ל-1, והשער נפתח רק כשגם המסלול נעול, כלומר בשלב 4. שום דבר שאושר בגרסה 2.2 לא בוטל.

גרסה 2.2, 14.09.2026: פער 29 שדוח שלב 2 פתח, בהכרעת בעלת הפרויקט. סעיף 5: נוספה שורת /index.html כנקודת הכניסה, ובכך נקלטה הכרעת פער 21 של שלב 1 (דרך א), שהוכרעה ב-14.09 ולא הגיעה לגוף המסמך בגרסה 2.1. שורת הסטטוס יושרה למספר הגרסה שביומן. שום דבר שאושר בגרסה 2.1 לא בוטל.

גרסה 2.1, 14.09.2026: שני פערים שדוח שלב 1 החזיר, בהכרעת בעלת הפרויקט. סעיף 3: שורת המאגר פוצלה, קוד ב-Git ומסמכים ודוחות בדרייב בתיקיית drafts. סעיף 5: ל-/tests/ נוספה הרמה structure, ונוספו /tests/helpers/ ו-/tests/run-all.js. שום דבר שאושר בגרסה 2 לא בוטל.

גרסה 2, 13.09.2026: שלושת התיקונים של findings-04, בהכרעת בעלת הפרויקט: (א) נספח עם פרומפטים 2, 3 ו-4 כלשונם ממנגנון הבנייה, ומחזור השלב; (ב) שלוש ההנחות שלא סומנו (השפה, שמות משתני הסביבה, עץ הקבצים) סומנו כהצעות, ועץ הקבצים אושר כהצעה מחייבת; נתוני ההדגמה ומצבי העצירה סומנו כדוגמה וכריכוז; (ג) שורת איסור חסרה בגבולות הסוכן: אין מודולי שירות לפני אישור המסכים, וארבע נקודות האישור של המנגנון נרשמו במפורש. שום דבר שאושר בגרסה 1 לא בוטל. גרסה 1, 12.09.2026: המסמך המקורי, נגזר ממפת המודולים גרסה 2 ומ-decision-04, וכולל את הכרעות סבב ההכרעות המרוכז שאושרו 12.09 (חלקים א עד ד). המסמך משתנה בידי בעלת הפרויקט בלבד, בגרסה חדשה.

השלב הבא: פרומפט 2, תוכנית שלב 4 (מודולי הליבה), בשיחה חדשה, לאישור לפני קוד ראשון.
