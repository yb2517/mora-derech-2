# תוכנית שלב 5: ממשקי הקלט והאוטומציה

סטטוס: טיוטה, ממתינה למילת אישור לפני כל כתיבה. פרומפט 2 של מנגנון הבנייה, ריצה שלישית.
תאריך: 14.09.2026
תפקיד: בניית שלב 5 לפי doc-module-map גרסה 3.3 סעיף 7 ו-CLAUDE.md גרסה 2.2 סעיף 6.
חוק ברזל: כל משימה כאן נגזרת משורה במפה או מדרישה ממוספרת. אין הרחבת היקף.

## 0. תנאי הסף שאינו מתקיים, ולכן נכתב ראשון

**המאגר מחזיק היום את שלבים 1 ו-2 בלבד. שלבים 3 ו-4 לא נבנו.** זו קביעה מן המאגר בפועל, לא מזיכרון השיחה:

| מה בדקתי | מה נמצא |
|---|---|
| `git log` | אחרון: "Stage 2: drop the stale .gitkeep from tools". אין קומיט של שלב 3 או 4 |
| `/services/` | ריקה, `.gitkeep` בלבד. אין governance.js, gate.js, retrieval.js, dialogue.js, delivery.js, log.js |
| `registry/modules.json` | ל-BE-03 עד BE-07 ול-FE-04 יש handler רשום, והקובץ שאליו הוא מצביע אינו קיים |
| `tests/run-all.js` | שלוש הבדיקות האדומות מסומנות "אינה ניתנת לכתיבה: שלב 4" |
| `tools/demo-modules.js` | כותב בגוף הקובץ: "כל מה שנראה כהכרעה עסקית שייך ל-BE-05, ל-BE-06 ול-BE-07, והוא נבנה בשלבים 3 ו-4" |

שלב 5 אינו יכול להיבנות מעליהם, מפני שכל אחד מששת המודולים שלו מדבר עם מודול שאינו קיים: AUTO-01 שולח arrive ו-leave ל-FE-04; AUTO-02 שולח release ל-FE-04 ו-close_stale ל-BE-07; CONN-01 משרת את ask של BE-03; CONN-02 מקבל את הטקסט מ-FE-04; חסימת הסוללה קוראת nearestExitPoint מ-BE-05. בדיקת הקבלה של השלב, הליכה מלאה בסימולטור שמייצרת 12 שורות arrive, אינה ניתנת להרצה בלי FE-04 ובלי BE-07: אין מי שימסור ואין מי שירשום.

לכן התוכנית הזאת מוגשת כתוכנית, ואין אחריה קוד. שלוש הדרכים, וההכרעה שלך:

| הדרך | מה קורה | המלצה |
|---|---|---|
| א | לבנות את שלב 3 ואז את שלב 4, ולפתוח את שלב 5 אחריהם, לפי סדר הבנייה במפה סעיף 7 | **מומלצת.** זה הסדר שהמפה קובעת, והתוכנית הזאת ממתינה כפי שהיא |
| ב | לשנות את סדר הבנייה במפה בהכרעת בעלת הפרויקט, ולבנות את שלב 5 מול מודול ההדגמה כפי שהמסכים נבנו בשלב 2 | אפשרית, ויקרה: מודול ההדגמה אינו מחליט ואינו זוכר, ולכן כל בדיקות הקבלה של השלב יהיו על תשובות קבועות, ויצטרכו להיכתב מחדש כששלב 4 ייבנה |
| ג | לבנות את מודולי השירות בתוך שלב 5 | **אינה אפשרית.** זו הרחבת היקף שסעיף 11 במסמך הבנייה אוסר במפורש |

יתר המסמך הוא תוכנית השלב המלאה, כדי שתהיה מוכנה לרגע שבו תנאי הסף מתקיים.

## 1. מה השלב מייצר

מפה סעיף 7, שלב 5: "ממשקי קלט ואוטומציה: CONN-01 עד CONN-03, AUTO-01, AUTO-02, TOOL-01, וחסימת הסוללה". זו הנקודה שבה המערכת מפסיקה לחכות ללחיצה: מקור הדגימות, הכרעת ה-geofence, מפעיל הזמן, שני ערוצי הקול, וההגנה שמכבה הכול כשהסוללה נגמרת. בסוף השלב אפשר להריץ הליכה מלאה בסימולטור מקצה לקצה, בלי לגעת במסך.

## 2. המשימות, בסדר ביצוע

משימה אחת בכל פעם, לפי מחזור השלב.

| # | המשימה | הדרישה או השורה במפה | המודול | הקבצים | בדיקת הקבלה: מה עושים, מה חייבים לראות |
|---|---|---|---|---|---|
| 1 | CONN-03, מתאם המיקום: דגימות מהמכשיר או מהסימולטור, באותה צורה בדיוק, במרווח שנקרא מ-reference | מפה 3.3 CONN-03; F-01 צעד 1; 2.4 location_sample_interval_s | CONN-03 | /connectors/location.js | דגימה מכל אחד משני המקורות נראית זהה: lat, lng, accuracy, time. דוחים את הרשאת המיקום: E-LOCATION-NOT-ALLOWED פעם אחת, והסשן מקבל את הדגל no_location (F-01 זרימה ד). מרוקנים את location_sample_interval_s: E-REF-EMPTY עם שם המפתח, ואין דגימה במרווח מומצא |
| 2 | TOOL-01, סימולטור ההליכה: הזרמת דגימות מלאכותיות אל CONN-03, וסימון כל מה שנולד מהן | מפה 3.3 TOOL-01; F-01 זרימה ה; מסמך הבנייה סעיף 6 שלב 5 | TOOL-01 | /tools/simulator.js | מריצים מסלול מלאכותי: הדגימות נכנסות בצורת CONN-03 ולא בצורה משלהן. כל שורת INTERACTIONS שנולדה מההרצה נושאת דגל simulator (BL-16). שורות tool-simulator ברשימת המותר: allowed = false בייצור (מפה 4.3) |
| 3 | AUTO-01, ה-geofence: סינון דיוק, מרחק לכל עוגני המסלול ולנקודות היציאה, בחירת הקרוב, arrive ו-leave | מפה 3.3 AUTO-01; BL-15; F-01 צעדים 2, 3, 4, 11 | AUTO-01 | /automation/geofence.js | דגימה בדיוק 80 מטר: אין אירוע, מפני ש-accuracy_threshold_m הוא 40 (מפה 6.1). שני עוגנים ברדיוס: arrive לקרוב בלבד. הקרוב הוא התחנה שכבר בזיכרון: אין אירוע. מרחק גדול מ-geofence_radius_m ועוד exit_margin_m מכל העוגנים: leave, והתחנה מתאפסת |
| 4 | AUTO-02, הטיימר: release למינון, close_stale לסשנים | מפה 3.3 AUTO-02; F-01 צעד 7; F-09 צעד 7; 2.4 stale_session_minutes | AUTO-02 | /automation/timer.js | פריט מוחזק: בתום delivery_gap_s נשלח release ל-FE-04, ו-FE-04 ממשיך מצעד 8. סשן פתוח בלי אינטראקציה במשך stale_session_minutes: נשלח close_stale ל-BE-07, והסשן נסגר. שני הערכים מ-reference, לא מהקוד |
| 5 | CONN-02, מתאם הקול: speak ו-stop, בחירת קול עברי מפורשת, שרשור קטעים, השהיה אחרי ביטול | מפה 3.3 CONN-02; F-02; מסמך הבנייה סעיף 2, שלוש ההתנהגויות שנבדקו | CONN-02 | /connectors/tts.js | שלוש ההתנהגויות נבדקות אחת אחת. מכשיר בלי קול עברי: E-NO-HEBREW-VOICE פעם אחת בסשן, הסשן מקבל את הדגל no_hebrew_voice, והמסך עובר לטקסט (displayed_as_text). voice_id ריק בטבלה: E-REF-EMPTY, ואין נפילה לקול ברירת מחדל |
| 6 | CONN-01, מתאם ההקשבה: listen, אודיו לטקסט או כשל | מפה 3.3 CONN-01; F-02, F-04 | CONN-01 | /connectors/stt.js | listen מחזיר טקסט. לא זוהה דיבור: E-SPEECH-NOT-RECOGNIZED. הרשאת מיקרופון נדחתה: E-MIC-NOT-ALLOWED, והמסך מציע הקלדה. האודיו אינו נשמר בשום שלב (BL-18) |
| 7 | חסימת הסוללה: עצירת מסירה, כיבוי מאזין המיקום ומנוע הקול, הודעת יציאה סטטית, וחידוש | מפה 2.3 BL-20; 2.4 ארבעת מפתחות הסוללה; מפה 7 שלב 5 | FE-05, קורא nearestExitPoint מ-BE-05 | /screens/traveler/ | מורידים את הסוללה ל-4%: המסירה נפסקת, שני המאזינים כבויים, וההודעה נבנית מ-battery_block_text ומנקודת היציאה הקרובה. למסלול אין נקודת יציאה: E-NO-EXIT-POINT, וההודעה אינה מומצאת. מעלים מעל battery_resume_percent: חידוש. הסשן נסגר עם ended_on_battery_block |
| 8 | מערך הבדיקות של השלב, ו-Regression על כל מה שנבנה | מפה 6.1 (ארבע שורות: AUTO-01 פעמיים, CONN-02, FE-05), 6.2, 6.3, 6.4; מסמך הבנייה חוק 9 | בדיקות | /tests/unit/, /tests/system/, /tests/structure/ | פקודה אחת מריצה הכל וירוקה. System: הליכה מלאה בסימולטור, 12 תחנות, 12 שורות arrive, כל שורה עם דגל simulator. Regression: 524 הטענות הקיימות ממשיכות לעבור, ומבחני המבנה 01, 03 עד 08 ממשיכים לעבור |
| 9 | דוח השלב לפי פרומפט 3, ומיזוג אחרי ירוק | מנגנון הבנייה, מחזור השלב | | דוח בטיוטות | הדוח מכיל את טבלת המשימות, תוצאות לפי רמות, מבחני המבנה, רשימת הקבצים, חוב טכני, וטבלת פערים |

סדר המשימות אינו שרירותי: 1 ו-2 יוצרים את מקור הדגימות, 3 הופך דגימה לאירוע, 4 מוסיף את מפעיל הזמן, 5 ו-6 פותחים את ערוץ הקול, ו-7 מכבה את כולם. כל משימה נבדקת לפני הבאה.

## 3. השערים וההכרעות הפתוחות שהשלב תלוי בהן

| # | השער או ההכרעה | המצב | מה הוא חוסם |
|---|---|---|---|
| 1 | שלבים 3 ו-4 לא נבנו (סעיף 0) | חוסם | את השלב כולו |
| 2 | הכרעה פתוחה 2 במפה: ספק הענן, חשבון האירוח והמסד | פתוחה. מפה סעיף 7 נוקבת בה במפורש כשער החוסם של שלב 5, ומסמך הבנייה סעיף 3 קובע ענן משלב 5 | יעד ההרצה של השלב, ודרייבר ה-Repository |
| 3 | voice_id ו-voice_rate בטבלת ה-reference | ריקים (null). מפה 2.4 כותבת "היום בקוד, צעד הבא: לכאן", והקוד ההוא אינו במאגר | משימה 5. CONN-02 יחזיר E-REF-EMPTY ולא ידבר |
| 4 | הכרעה פתוחה 5: חידוש סשן אחרי טעינה | פתוחה. הייתה השער החוסם של שלב 4, ושלב 4 לא רץ | משימה 4: close_stale אינו יודע מה לעשות בסשן שחזר |
| 5 | כניסה חוזרת לאותה נקודה (F-01 הכרעה פתוחה 8) ופריטים נוספים באותה נקודה (הכרעה 4) | פתוחות, מסומנות [הצעה] במקרה השימוש ולא נקלטו למפה | משימה 3: התנהגות AUTO-01 בכניסה חוזרת אינה מוגדרת |
| 6 | ההכרעה שנפתחת כאן: איך מגיעים אל המתאמים (פער 34) | פתוחה | משימות 1, 2, 5, 6 |

## 4. מה במפורש מחוץ להיקף השלב

- GW-01 והתפר הריק אל ספק ה-AI: שלב 6.
- ניקוי נתוני ההדגמה, העברת 19 פריטי הקורפוס לרשומות, ואימות העוגנים ונקודות היציאה בשטח: שלב 7.
- דרייבר הענן (/repository/driver-cloud.js): תלוי בהכרעה פתוחה 2, ואינו נבנה בלי הכרעה.
- F-14, ייצוג המסלול כקו וזיהוי חריגה: נדחה מ-v1 בהכרעת 12.09.
- כל שינוי במפת המודולים או במסמך הבנייה: פרומפט 4 בלבד, בהכרעת בעלת הפרויקט.
- מודולי השירות עצמם (BE-03 עד BE-07, FE-04): שלבים 3 ו-4, ולא כאן.

## 5. הפערים שהתוכנית חושפת

ממשיכים את המספור מפער 31, שנסגר בגרסה 3.3 של המפה. כל אחד מהם נרשם ואינו מתוקן בקוד, לפי סעיף 9 במסמך הבנייה.

| # | הפער | ההפניה המדויקת | ההכרעה המוצעת |
|---|---|---|---|
| 32 | בקשה לפתוח את שלב 5 בעוד שלבים 3 ו-4 לא נבנו | מפה 7, סדר הבנייה; מסמך הבנייה 6 | דרך א בסעיף 0: לבנות 3 ואז 4. שינוי הסדר, אם ייבחר, הוא הכרעת בעלת הפרויקט ונרשם במפה לפני הקוד |
| 33 | מסמך הבנייה סעיף 6 שלב 5 אינו מזכיר את חסימת הסוללה, והמפה סעיף 7 כן | מסמך הבנייה 6 מול מפה 7 | המפה גוברת (כלל העל). שורת שלב 5 בסעיף 6 במסמך הבנייה מתוקנת ומקבלת את חסימת הסוללה ואת BL-20 |
| 34 | CONN-01, CONN-02 ו-CONN-03 אינם מופיעים ב-4.2 (אין להם פעולה), אינם ברשימה הסגורה של הפונים ב-4.1, ואין להם שורה ברשימת המותר. modules.json רושם אותם בלי handler ובלי caller. גם TOOL-01 אמור להזרים דגימות אל CONN-03, ואין לכך מסלול בחוזה | מפה 4.1, 4.2, 4.3; registry/modules.json; חוקי הברזל 2 ו-7; מבחן מבנה 08 | הצעה: המתאם הוא תלות מוזרקת של המודול שמחזיק אותו (CONN-03 ל-AUTO-01, CONN-02 ו-CONN-01 ל-FE-05 ול-BE-03), כפי שהדרייבר מוזרק ל-Repository, ולא מודול שפונים אליו במעטפה. זו הכרעה על משמעות הליבה, ולכן היא עולה לבעלת הפרויקט ואינה מוכרעת בקוד |
| 35 | AUTO-01 נדרש לחשב מרחק לכל עוגני המסלול ולנקודות היציאה, ואין ל-BE-05 פעולה שמחזירה אותם ל-module-geofence. ברשימת המותר יש ל-module-geofence שתי שורות בלבד, arrive ו-leave | מפה 4.2 (אין שורה), F-01 צעד 3; registry/allow-list.json | זהה במבנהו לפער 30 שנסגר בגרסה 3.2: נדרשת פעולת קריאה ל-BE-05 ושורה ברשימת המותר. השם המוצע: listAnchors, ולצדה listExitPoints לפונה module-geofence |
| 36 | F-13 תיקון 1 אינו במאגר, ו-BL-20, ארבעת מפתחות הסוללה, ישות EXIT_POINTS ושלוש פעולות של BE-05 נגזרים ממנו | מפה 2.3 BL-20, 2.4, 4.2; docs/usecase-f-13.md מסתיים בגרסה שלפני התיקון | המסמך מועלה למאגר לפני משימה 7, או שמשימה 7 נבנית מן המפה בלבד ומסומנת ככזו בדוח |
| 37 | אב הטיפוס prototype-mora-darech-jaffa-v1.html אינו במאגר, ומסמך הבנייה סעיף 2 קובע אותו כמקור לשלוש התנהגויות הקול, לסימולטור ההליכה ול-19 פריטי הקורפוס | מסמך הבנייה סעיף 2; data/reference.json, השדה pending.voice_id, שכבר רשם זאת בשלב 1 | הקובץ מועלה למאגר לפני משימות 2 ו-5. בלעדיו שתי המשימות נכתבות מן המפה בלבד, ושלוש התנהגויות הקול אינן ניתנות לאימות מול מה שנבדק |
| 38 | בדיקת הקבלה של השלב דורשת הליכה של 12 תחנות, ונתוני ההדגמה מחזיקים מסלול אחד עם שלוש תחנות, ארבעה עוגנים ונקודת יציאה אחת. הקורפוס נכנס לרשומות בשלב 7 | מסמך הבנייה 6 שלב 5 ו-7; data/demo/demo-data.json | הצעה: נתוני ההדגמה מורחבים ל-12 תחנות מסומנות is_demo, ובדיקת הקבלה רצה עליהן. החלופה, לדחות את בדיקת הקבלה לשלב 7, משאירה את שלב 5 בלי בדיקת הקבלה שהמפה קובעת לו |

## 6. הערה על הענף

מחזור השלב במסמך הבנייה קובע ענף נפרד לכל שלב. ההנחיה התפעולית שקיבלתי לשיחה הזאת מחייבת אותי לענף stage-2, ולכן המסמך הזה נכתב שם. פער תפעולי, לא פער במסמכים, ונרשם כאן כדי שלא יישאר שקט.

## 7. מה נדרש ממך כדי לפתוח

1. הכרעה בסעיף 0: דרך א, ב או ג.
2. הכרעה בשבעת הפערים 32 עד 38, או לפחות ב-32, 34 ו-35, שבלעדיהם אין למשימות 1 עד 3 מסלול בחוזה.
3. מילת אישור לתוכנית עצמה, לפני כל כתיבת קוד.

זהו תוצר השלב. אין קוד עד לאישורך.
