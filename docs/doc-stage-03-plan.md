# תוכנית שלב 3: הפרוסה הראשונה, F-07 מקצה לקצה

סטטוס: מאושרת בהוראת בעלת הפרויקט, 14.09.2026 ("build stage 3", אחרי ההכרעה בדרך א של תוכנית שלב 5). פרומפט 2 של מנגנון הבנייה.
תאריך: 14.09.2026
תפקיד: בניית שלב 3 לפי doc-module-map גרסה 3.3 סעיף 7 ו-CLAUDE.md גרסה 2.2 סעיף 6.
חוק ברזל: כל משימה כאן נגזרת משורה במפה או מדרישה ממוספרת. אין הרחבת היקף.

## 1. מה השלב מייצר

מפה סעיף 7, שלב 3: "Slice ראשון: F-07 מקצה לקצה דרך ה-Orchestrator, כולל register_mou ו-M-06 מרשומה". זו הפעם הראשונה שבקשה של מסך משנה נתון אמיתי: עד היום מודול ההדגמה אישר קבלה ולא זכר דבר. בסוף השלב החוקר מאשר פריט, המצב משתנה, רשומת APPROVALS נכתבת, וכשל בכתיבתה מבטל את המעבר.

תנאי הסף מתקיים: שלבים 1 ו-2 במאגר, והמערך ירוק ב-524 טענות.

## 2. המשימות, בסדר ביצוע

| # | המשימה | הדרישה או השורה במפה | המודול | הקבצים | בדיקת הקבלה: מה עושים, מה חייבים לראות |
|---|---|---|---|---|---|
| 1 | הישויות העסקיות בשכבת הנתונים: הדרייבר מכיר את שש הטבלאות של F-07, ול-Repository נוספות פעולות בשם עסקי. שינוי ליבה מבוקר לפי סעיף 9.2 במסמך הבנייה: Commit נפרד ומסומן, עם Regression מלאה | מפה 3.1 CORE-04; BL-09; F-07 סעיף 8 (השמות העסקיים); repository/index.js, שכבר כתב "הן נכנסות בשלב 3" | CORE-04 | /repository/driver-browser.js, /repository/index.js | חיפוש מחרוזת החיבור: עדיין קובץ אחד (מבחן מבנה 01). APPROVALS: הוספה בלבד, ואין בממשק פעולה שמוחקת או דורסת רשומה. Regression: 524 הטענות עוברות |
| 2 | טבלת המעברים בליבה: /core/business-logic.js עם טבלה 2.2, BL-01 ו-BL-02, ובשלב הזה בלבד את מה ש-F-07 דורש | מפה 2.2, 2.3 (BL-01, BL-02); CLAUDE.md סעיף 5; F-07 סעיף 8 ("בתנאי שהמעברים בליבה") | הליבה | /core/business-logic.js | מעבר שאינו בטבלה: נדחה. הטבלה יושבת בליבה, ו-BE-05 אינו מחזיק עותק שלה |
| 3 | BE-05, ארבעת המעברים של F-07: submit, approve, reject, return, כל אחד עם רשומת APPROVALS לפני שינוי המצב | מפה 4.2 (ארבע שורות screen-veto); F-07 צעדים 3, 4, 8, 9; BL-01, BL-02 | BE-05 | /services/governance.js | מאשרים פריט pending: המצב approved, ורשומת APPROVALS עם זמן, מבצע, מצב קודם וחדש. approve על פריט approved: E-TRANSITION-DENIED, ואין רשומה חדשה. submit לפריט בלי עמוד: E-ITEM-INCOMPLETE עם שם השדה ב-error.data. **מכשילים את כתיבת ה-APPROVALS: E-APPROVAL-WRITE-FAILED, ומצב הפריט אינו משתנה** (BL-01) |
| 4 | BE-05, פעולות הקריאה שהפרוסה צריכה: listItems, getItem, listApprovals, getSite, listSources, listInstitutes, listMou | מפה 4.2 כפי שתוקנה בפער 30 (גרסה 3.2); F-07 צעדים 5, 11, 12 | BE-05 | /services/governance.js | המסך מציג את הפריטים מהרשומות ולא מההדגמה. קריאה שאינה מוצאת: רשימה ריקה או null עם ok, ולא קוד שגיאה |
| 5 | BE-05, register_mou: רשומת RIGHTS_MOU עם מכון, היקף, חתימה ותוקף | מפה 4.2 (screen-owner); F-07 צעדים 12, 13; decision-02 | BE-05 | /services/governance.js | רושמים הסכם: הרשומה קיימת, וקישורה למקורות נשמר. הסכם בלי מכון או בלי היקף: E-ITEM-INCOMPLETE עם שם השדה |
| 6 | BE-06, get_gate עם M-06 מרשומות: מספר המכונים עם הסכם בתוקף שמכסה 100% ממקורות הפריטים המאושרים, ומצב שער B | מפה 3.2 BE-06, BL-08; F-07 צעדים 10, 14; F-08 L4; 2.4 enforce_gate_b | BE-06 | /services/gate.js | M-06 נקרא מרשומות ולא מקבוע (מבחן ההחלפה של F-07 סעיף 8, שנכשל היום). שער B פתוח כאשר locked ו-M-06 גדול או שווה 1. enforce_gate_b כבוי: המצב מוצג ואינו נאכף |
| 7 | ההרכבה: זריעת הישויות מנתוני ההדגמה, והחזרת ההדגמה לפי פעולה ולא לפי מודול | מסמך הבנייה סעיף 7 (נתוני הדגמה מסומנים); index.html, שכבר כתב "ברגע ש-services/governance.js ייכנס בשלב 3" | תשתית | /index.html | פעולה שנבנתה מגיעה למודול האמיתי; פעולה שלא נבנתה עדיין מגיעה להדגמה ומסומנת is_demo. אין מודול שמייבא מודול (מבחן מבנה 08) |
| 8 | המסכים: פאנל הווטו נטען מחדש אחרי מעבר, ומסך הניהול מקבל טופס הסכם | F-07 צעדים 11, 12; מסמך הבנייה סעיף 6 שלב 3 ("רושמים הסכם: השער נפתח על המסך") | FE-06, FE-08 | /screens/veto/, /screens/admin/ | אחרי אישור, המסך מציג את המצב החדש ואת הרשומה החדשה בלי רענון ידני. רושמים הסכם בטופס: פס השערים מציג את השער כפתוח |
| 9 | מערך הבדיקות של השלב, ו-Regression מלאה | מפה 6.1 (שורות BE-05 ו-BE-06), 6.2, 6.3; מסמך הבנייה חוק 9 | בדיקות | /tests/unit/, /tests/system/ | פקודה אחת ירוקה. System: F-07 מקצה לקצה דרך ה-Orchestrator, כולל זרימה ב (כשל כתיבת היומן) וזרימה ג (מסך שאינו מורשה) |
| 10 | דוח השלב לפי פרומפט 3 | מנגנון הבנייה | | /docs/ | טבלת המשימות, תוצאות לפי רמות, מבחני המבנה, קבצים, חוב טכני, וטבלת פערים |

## 3. מה במפורש מחוץ להיקף השלב

- יתר פעולות BE-05: create_item, edit_item, verify_anchor, register_source, register_institute, register_exit_point, lock_site, listApprovedByStop, nearestExitPoint, listExitPoints. שלבים 4 ו-5, ועד אז ההדגמה עומדת במקומן.
- L1 עד L6 ותנאי הנעילה: BL-06, שלב 4.
- get_lock_readiness ו-set_enforce של BE-06: שלב 4.
- BE-04, BE-03, FE-04, BE-07: שלב 4. שלוש הבדיקות האדומות תלויות בהם ונשארות מסומנות.
- הסרת נתוני ההדגמה: שלב 7.

## 4. השערים וההכרעות שהשלב תלוי בהן

| # | ההכרעה | מצב | מה עשיתי |
|---|---|---|---|
| 1 | אימות זהות המבצע: אין ב-v1 (הוכרע 12.09) | סגורה, ומשאירה את APPROVALS.who בלי אדם | who נרשם משם הפונה שבמעטפה, שהוא מה שהמערכת באמת יודעת. נרשם כפער |
| 2 | שם שדה התוקף ב-RIGHTS_MOU (decision-02) | [טרם נקבע] במפה | נקרא valid_until, כפי שנתוני ההדגמה כבר כתבו. נרשם כפער |
| 3 | SITES.bounds מסומן [הצעה] במפה ואינו קיים ברשומות | פתוחה | תנאי השלמות של submit בודק טקסט, עמוד, תחנה, מקור ועוגן. בדיקת הגבולות אינה ניתנת לאכיפה בלי השדה, ונרשמת כפער |

זהו תוצר הצעד. הבנייה מתחילה במשימה 1.
