# תיקון המסמכים מפערי שלב 3

סטטוס: טיוטה, ממתינה למילת אישור. פרומפט 4 של מנגנון הבנייה, ריצה ראשונה בפרויקט.
תאריך: 14.09.2026
המקור: טבלת הפערים בדוח שלב 3 (doc-stage-03-report.md סעיף 5), שבעה פערים.
חוק ברזל: מתוקן רק מה שעולה מהפערים. אין הוספת מודולים, פעולות או יכולות שאיש לא ביקש. פער שמערער על הליבה חוזר אלייך כהחלטת אפיון ואינו מתוקן כאן.

הערה על הסמכות: סעיף 11 ב-CLAUDE.md אוסר עליי לערוך את המפה ואת מסמך הבנייה. לכן המסמך הזה הוא **הצעה לשתי גרסאות חדשות**, עם הנוסח המדויק לפני ואחרי. אחרי מילת האישור שלך אני מחיל אותן על הקבצים, מעדכן את הנתונים והבדיקות שנגזרים מהן, ומעתיק ל-prd-update-inbox.

## 1. לאן הולך כל פער

| # | הפער | היעד |
|---|---|---|
| 32 | אין מקור לשדה who של APPROVALS | **מפוצל**: הבהרה במפה 2.1 על מה שנרשם ב-v1, ו**החלטת אפיון שחוזרת אלייך** על שדה זהות במעטפה |
| 33 | "מודול שירות חלקי" אינו מוגדר | מפה, הערה בסעיף 3.2 |
| 34 | סתירה: "השער נפתח על המסך" מול BL-08 | CLAUDE.md סעיף 6, שורת שלב 3 |
| 35 | screen-owner ו-get_gate: 4.2 מול 4.4 | מפה 4.2, חידוד עמודת הפונה |
| 36 | עמודת "מי רשאי" בטבלה שבליבה מול 4.3 ומבחן 06 | מפה 2.2, הערה אחרי הטבלה |
| 37 | אין קוד ל"פריט לא נמצא" ול"רשומה נכתבה והמצב לא" | מפה 4.5, חידוד שורת E-TRANSITION-DENIED. **הרשימה הסגורה נשארת 23 קודים** |
| 38 | הערה עד 200 תווים בלי מפתח ב-2.4 | מפה 2.4, מפתח note_max_chars |

שישה פערים מתוקנים בשתי גרסאות מסמך. אחד, החצי השני של 32, אינו מתוקן כאן.

## 2. מפת המודולים: גרסה 3.4 המוצעת

### 2.1 פער 36, סעיף 2.2, ההערה שאחרי הטבלה

**היום**, שורה אחת אחרי טבלת המעברים:
> כל מעבר אחר נדחה ב-E-TRANSITION-DENIED ונרשם.

**המוצע**, אותה שורה ואחריה פסקה:
> כל מעבר אחר נדחה ב-E-TRANSITION-DENIED ונרשם.
>
> עמודת "מי רשאי (from)" היא תיעוד, ואינה המקום שבו ההרשאה נאכפת. סעיף 4.3 קובע "הוספת מסך או פונה: שורה, לא קוד", ומבחן מבנה 06 אוכף שאין שם פונה בקוד המערכת, ולכן שם הפונה יושב ברשימת המותר בלבד, ו-CORE-02 בודק אותו לפני שהבקשה מגיעה ללוגיקה (BL-11 צעד 3). הטבלה שבליבה נושאת את המצבים, את הפעולה, ואת ההבחנה שרשימת המותר אינה יכולה לבטא: מעבר שמגיע במעטפה מול מעבר שהמערכת מבצעת מעצמה, כמו revert. לכל שורה כאן שנושאת שם מסך יש שורה מקבילה ב-4.3, ובדיקת ממשקים מצליבה את השתיים כדי שלא ייפרדו.

הנימוק: שתי שורות במפה משכו לשני כיוונים, ומבחן מבנה 06 תפס את זה בקוד. ההכרעה אינה משנה התנהגות: היא אומרת איפה הכלל גר.

### 2.2 פער 32, החצי שמתוקן כאן: סעיף 2.1, שורת APPROVALS

**היום**:
> | APPROVALS (יומן ההחלטות) | approval_id, time, who, target (item_id או site_id), action, from_status, to_status, note | append-only, אין מצב | נכתב בלבד, בידי BE-05, בכל מעבר מצב. אין עריכה ואין מחיקה |

**המוצע**:
> | APPROVALS (יומן ההחלטות) | approval_id, time, who (ב-v1: שם הפונה מהמעטפה, אחרי אימות מול רשימת המותר, ולא זהות אדם), target (item_id או site_id), action, from_status, to_status, note | append-only, אין מצב | נכתב בלבד, בידי BE-05, בכל מעבר מצב. אין עריכה ואין מחיקה |

הנימוק: השדה קיים במפה, המעטפה של 4.1 אינה נושאת זהות, ואין ב-4.2 פעולה שקוראת ADMIN_USERS. המפה אומרת עכשיו מה באמת נרשם, במקום להשאיר קורא להסיק שנרשם שם של אדם.

### 2.3 פער 33, סעיף 3, ההערה שלפני טבלאות המודולים

**היום**:
> מצב הביצוע לפי שלושת המבחנים בתבנית. ב-v1 אין Agent ואין רשת סוכנים: כל דרגות המימוש "קוד רגיל".

**המוצע**, אותה פסקה ואחריה:
> מצב הביצוע לפי שלושת המבחנים בתבנית. ב-v1 אין Agent ואין רשת סוכנים: כל דרגות המימוש "קוד רגיל".
>
> "חלקי" בעמודת מצב הבנייה הוא מצב מוכר ולא זמני בלבד: מודול נבנה בהיקף השלב שלו, ומממש את הפעולות של מקרה השימוש שהשלב מביא. פעולה של אותו מודול שטרם נבנתה אינה מקבלת תשובה ואינה מדמה אחת: היא חוזרת כ-E-MODULE-FAILED דרך CORE-02, נרשמת ב-audit_log, ונראית על המסך. מודול שיענה תשובה מנומסת על פעולה שלא נבנתה יסתיר את מה שחסר.

### 2.4 פער 35, סעיף 4.2, שורת BE-06

**היום**:
> | BE-06 | get_gate, get_lock_readiness | F-07 צעד 10, F-08 צעד 9 | screen-veto, screen-owner, screen-traveler (get_gate) |

**המוצע**, שתי שורות במקום אחת:
> | BE-06 | get_gate | F-07 צעד 10 | screen-veto, screen-traveler |
> | BE-06 | get_lock_readiness | F-08 צעד 9 | screen-owner |

הנימוק: 4.4 מונה את פעולות כל מסך "ואותן בלבד", ושם ל-screen-owner אין get_gate ול-screen-veto אין get_lock_readiness. הפיצול מיישר את 4.2 ל-4.4 ואינו מוסיף הרשאה לאיש. **אם תרצי שמצב השער יוצג גם במסך בעלת הפרויקט, זו תוספת ל-4.4 ושורה ברשימת המותר, והיא החלטה שלך ואינה נכנסת כאן.**

### 2.5 פער 37, סעיף 4.5, שורת E-TRANSITION-DENIED

**היום**:
> | E-TRANSITION-DENIED | מעבר מצב שאינו בטבלה 2.2 | BE-05 |

**המוצע**:
> | E-TRANSITION-DENIED | מעבר מצב שאינו בטבלה 2.2, ובכלל זה בקשת מעבר על פריט שאינו קיים: אין לו מצב נוכחי, ולכן אין לו שורה בטבלה | BE-05 |

**הרשימה הסגורה נשארת 23 קודים.** לא נוסף קוד ל"פריט לא נמצא" ולא ל"רשומה נכתבה והמצב לא השתנה": הראשון מכוסה בשורה הזאת, והשני הוא כשל מודול ש-E-MODULE-FAILED כבר מכסה, והרשומה ביומן נשארת עדות למה שנוסה.

### 2.6 פער 38, סעיף 2.4, מפתח חדש

**המוצע**, שורה אחת אחרי question_max_chars:
> | note_max_chars | 200 | FE-06, BE-05 | usecase-f-07 צעד 6 |

הנימוק: מקרה השימוש קובע "הערה עד 200 תווים", ל-2.4 לא היה מפתח למספר הזה, ו-BL-12 אוסר ערך משתנה בקוד. שלושת המסמכים יחד לא אפשרו לאכוף את הגבול בשום מקום. מבחן מבנה 05 תפס את הניסיון לכתוב 200 בקוד, והמפתח סוגר את המעגל.

**החלופה, אם תעדיפי**: ויתור על הגבול, ואז מקרה השימוש מתוקן ולא המפה. ההמלצה היא המפתח: הגבול נכתב בזמן שהמסך תוכנן, ונראה שהוא שם כדי שההערה תישאר קריאה ביומן.

### 2.7 שורת היומן של הגרסה החדשה

> גרסה 3.4, 14.09.2026: חמישה פערים שדוח שלב 3 החזיר, בהכרעת בעלת הפרויקט. 2.1: שורת APPROVALS אומרת מה נרשם בשדה who ב-v1 (פער 32). 2.2: נוספה הערה שקובעת ששם הפונה נאכף ברשימת המותר ולא בטבלה שבליבה, ושהטבלה מבחינה בין מעבר שמגיע במעטפה למעבר אוטומטי (פער 36). 2.4: נוסף המפתח note_max_chars בערך 200 (פער 38). 3: נוספה הערה שמגדירה מודול שנבנה בהיקף שלב, ופעולה שטרם נבנתה שחוזרת כ-E-MODULE-FAILED (פער 33). 4.2: שורת BE-06 פוצלה לשתי שורות, כדי ליישר את עמודת הפונה ל-4.4 (פער 35). 4.5: שורת E-TRANSITION-DENIED חודדה לכלול בקשת מעבר על פריט שאינו קיים, והרשימה נשארה 23 קודים (פער 37). שום דבר שאושר בגרסה 3.3 לא בוטל.

## 3. מסמך הבנייה: גרסה 2.3 המוצעת

### 3.1 פער 34, סעיף 6, שורת שלב 3

**היום**:
> | 3 | Slice ראשון: F-07 מקצה לקצה, כולל register_mou ו-M-06 מרשומה | החוקר מאשר פריט, ורואה רשומת APPROVALS עם זמן, מבצע, מצב קודם וחדש. מכשילים את כתיבת ה-APPROVALS: מצב הפריט אינו משתנה (BL-01). רושמים הסכם: השער נפתח על המסך |

**המוצע**:
> | 3 | Slice ראשון: F-07 מקצה לקצה, כולל register_mou ו-M-06 מרשומה | החוקר מאשר פריט, ורואה רשומת APPROVALS עם זמן, מבצע, מצב קודם וחדש. מכשילים את כתיבת ה-APPROVALS: מצב הפריט אינו משתנה (BL-01). רושמים הסכם: M-06 עובר מ-0 ל-1 ומחושב מהרשומה, והמסך מציג את מצב השער ואת מה שחסר לפתיחתו. השער עצמו נפתח רק כשגם המסלול נעול (BL-08), והנעילה היא שלב 4 |

הנימוק: BL-08 במפה קובע שני תנאים, נעילה ו-M-06 גדול או שווה 1, והנעילה שייכת ל-F-08 בשלב 4. בדיקת הקבלה כפי שנוסחה דרשה משלב 3 דבר שהמפה אינה מאפשרת לו לעשות. **המפה גוברת, ולכן מתוקן מסמך הבנייה ולא המפה.**

### 3.2 שורת היומן ושורת הסטטוס

> סטטוס: גרסה 2.3, מאושרת 14.09.2026.

> גרסה 2.3, 14.09.2026: פער 34 שדוח שלב 3 פתח, בהכרעת בעלת הפרויקט. סעיף 6: בדיקת הקבלה של שלב 3 נוסחה מחדש כך שתתיישב עם BL-08 במפה: רישום הסכם מביא את M-06 ל-1, והשער נפתח רק כשגם המסלול נעול, כלומר בשלב 4. שום דבר שאושר בגרסה 2.2 לא בוטל.

## 4. מה חוזר אלייך כהחלטת אפיון, ואינו מתוקן כאן

**פער 32, החצי השני: זהות המשתמש במעטפה.**

הפער עצמו: מפה 2.1 מגדירה ADMIN_USERS עם user_id ו-role, ו-APPROVALS עם שדה who, אבל המעטפה של 4.1 אינה נושאת זהות, ואין ב-4.2 פעולה שקוראת ADMIN_USERS. כלומר המערכת יודעת איזה **מסך** אישר, ולא איזה **אדם**.

למה זה אינו מתוקן בפרומפט 4: הוספת שדה למעטפה היא שינוי ליבה, וסעיף 9.1 ב-CLAUDE.md מחייב שהוא יעלה אלייך ולא ייפתר בקוד. שלוש הדרכים:

| הדרך | מה נדרש | מה המחיר |
|---|---|---|
| א. v1 נשאר כך | כלום. הרשומה אומרת screen-veto | Audit trail שאינו עונה על "מי אישר" בקריטריוני הקבלה של הסיפור. מספיק כל עוד יש חוקר אחד |
| ב. שדה actor במעטפה | שינוי 4.1, שינוי CORE-01, Regression מלאה | ליבה. ובלי אימות זהות זו הצהרה ולא ראיה |
| ג. סיסמה ואימות לפני שהפאנל יוצא למכון | הוכרע כבר ב-12.09 שזה נדרש אז, ולא נקבע מתי | הדרך היחידה שבה who אמיתי |

ההמלצה: **א לעכשיו, ג לפני שהפאנל יוצא מהבית**. ב לבדו נותן תחושת זהות בלי זהות.

זו החלטה שלך, והיא אינה חוסמת את שלב 4.

## 5. הצהרה: מה לא השתנה

- **הליבה לא נגעה.** המעטפות של 4.1 זהות, חמשת צעדי ה-Orchestrator זהים, ורשימת קודי השגיאה נשארה 23 קודים בדיוק. לא נוסף קוד ולא הוסר קוד.
- **לא נוסף מודול, לא נוספה פעולה, ולא נוספה יכולת.** שורת BE-06 ב-4.2 פוצלה לשתיים ולא גדלה: אותן שתי פעולות, אותם פונים.
- **לא נוספה שורה לרשימת המותר**, ולכן אף מסך לא קיבל הרשאה שלא הייתה לו.
- **החוקים העסקיים BL-01 עד BL-21 לא השתנו.** BL-08 לא שונה: מסמך הבנייה יושר אליו.
- **טבלת המעברים 2.2 לא השתנתה.** שמונה השורות כפי שהן, ונוספה הערה שאומרת איפה נאכפת עמודה אחת מהן.
- **גבולות ה-MLP לא השתנו.** שום דבר לא נכנס לגרסה 1 ושום דבר לא יצא ממנה.
- **סדר הבנייה של סעיף 7 לא השתנה.** שלב 4 נשאר מה שהוא.
- ההכרעות הפתוחות 1 עד 10 בסעיף 8 נשארות פתוחות כפי שהן.

## 6. מה קורה אחרי מילת האישור

1. החלת שתי הגרסאות על הקבצים: doc-module-map-v3.md לגרסה 3.4, ו-CLAUDE.md לגרסה 2.3.
2. הנתונים שנגזרים מהמפה: המפתח note_max_chars נכנס ל-/data/reference.json בערך 200, ובדיקת ה-reference מעודכנת לשלושים ואחד מפתחות.
3. אכיפת הגבול עצמו נכנסת לתוכנית שלב 4 כשורת משימה, ואינה נדחפת לאחור לשלב 3: דוח שלב 3 נשאר נאמן למה שרץ.
4. העתקה ל-prd-update-inbox.
5. מיזוג stage-3 ל-main, ושיחה חדשה לשלב 4 שנפתחת בפרומפט 2.

זהו תוצר השלב. ממתין למילת האישור שלך, בנפרד לכל פער אם תרצי.
