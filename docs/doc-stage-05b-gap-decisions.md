# תיקון המסמכים מפערי שלב 5, חלק ב

סטטוס: אושר 16.09.2026 בידי בעלת הפרויקט, כולל פער 63 בדרך ג. הגרסאות הוחלו במאגר. פרומפט 4 של מנגנון הבנייה, ריצה רביעית בפרויקט.
תאריך: 16.09.2026
המקור: doc-stage-05b-report.md סעיפים 5 ו-6 (הפערים 58 עד 63) ורישום הפערים סעיף 5א.
חוק ברזל של הצעד: מתקנים רק את מה שעולה מהפערים. לא נוספו מודולים, פעולות או יכולות שאיש לא ביקש. פער שמערער על הליבה או על גבולות ה-MLP חוזר אלייך כהחלטת אפיון, ולא מתוקן כאן. פער 63 הוא כזה, והוא מובא עם הצעה ובלי נוסח סופי.

## 1. ההכרעות, בשורה אחת לכל פער

| # | הפער | ההכרעה המוצעת | המסמך והסעיף שמשתנה |
|---|---|---|---|
| 58 | BL-01 ו-BL-10 מנוסחים מול "כשל כתיבה", ומול דרייבר ענן עם תור כתיבה הכשל שהחוק רואה הוא בזיכרון ולא במסד | החוק נאכף מול ה-Repository, ולא מול מקום האחסון. ה-Repository מחזיר כשל כשהדרייבר מחזיר כשל, ודרייבר עם תור מתחייב שלא לאבד שורה: היא נשמרת בתור עד שהמסד קיבל אותה, ודחייה קבועה נרשמת ומדווחת. BL-01 ו-BL-10 אינם משתנים במהותם; נוסף משפט הבהרה לכל אחד | מפה 2.3, שורות BL-01 ו-BL-10 |
| 59 | מבחן מבנה 01 "חיבור לנתונים בקובץ אחד", ומשלב 5 יש שני דרייברים, כל אחד בקובץ אחד | הנוסח: "כל דרייבר אחסון בקובץ אחד: אחסון הדפדפן בקובץ אחד, ומחרוזת החיבור לענן בקובץ אחד, ואף קובץ מחוץ ל-CORE-04 אינו נוגע באף אחד מהם". decision-05 סעיף 6 כבר אומר זאת; הבדיקה כבר מפוצלת | מפה 6.4, מבחן 01 |
| 60 | מפה 2.1 אינה מגדירה מפתח ראשי לישויות, ומסד דורש | שדה המזהה של כל ישות (הראשון ברשימת השדות) הוא המפתח הראשי שלה. audit_log: request_id ו-phase יחד; reference: המפתח. אין שדה חדש | מפה 2.1, פסקה חדשה אחרי טבלת הישויות |
| 61 | decision-05 מטיל על הספק גם את "הכתובת לטלפון", והדומיין של הספק אינו מגיש דף HTML שרץ | **הוכרע בידך 16.09.2026**: הכתובת לטלפון היא GitHub Pages של המאגר, שהפך לציבורי, בתהליך Actions שאורז מסודות המאגר. הספק מכסה את המסד בלבד | decision-05 גרסה 1.1, סעיפים 1, 5 ו-6; מסמך הבנייה סעיף 3 |
| 62 | .github/workflows/ אינה בעץ הקבצים של מסמך הבנייה סעיף 5, וכך גם repository/migrations/ שנוספה במשימה 13.2 | שתי התיקיות נכנסות לעץ, מסומנות כהגדרות פריסה ומסד ולא כקוד המערכת: מבחני המבנה אינם סורקים אותן, ואין בהן לוגיקה | מסמך הבנייה סעיף 5 |
| 63 | ארבעת המסכים מוצגים זה מתחת לזה בעמוד אחד, ואף מסמך אינו קובע כיצד משתמש מגיע למסך שלו | **הצעה, להכרעתך**: נקודת הכניסה בוחרת מסך אחד לפי פרמטר בכתובת (screen עם אחד מארבעת שמות הפונים), ובלי פרמטר מציגה את הרכבת הפיתוח כמו היום. המטייל מקבל קישור עם screen=screen-traveler ורואה את מסכו בלבד. אין מודול, פעולה, שורה ברשימת המותר או קוד שגיאה חדשים; רשימת המותר ממשיכה לקבוע מה כל מסך רשאי לבקש | מפה 4.3, פסקה חדשה; מסמך הבנייה סעיף 5, שורת index.html. **רק אם תאשרי** |

ארבעה מהפערים הם תיקוני נוסח שמתארים התנהגות שכבר נבנתה (58, 59, 60, 62). פער 61 מתעד הכרעה שכבר התקבלה. פער 63 בלבד גוזר שינוי קוד, בנקודת הכניסה בלבד, ורק אם תאשרי.

## 2. למה 58 ו-63 הוכרעו כך, ולא אחרת

**פער 58, שלוש הדרכים**: (א) לשנות את BL-01 כך שידרוש אישור מהמסד לפני המעבר: זה מחייב Repository אסינכרוני, כלומר שינוי בכל מודול ומסך, וזו נגיעה בליבה שהוכרע לא לעשות (פער 25, הכרעה ב2 בתוכנית חלק ב). (ב) להשאיר את BL-01 כלשונו ולומר שאינו חל על דרייבר הענן: זה מוחק את ההגנה בדיוק במקום שבו היא חשובה. (ג) לקבוע היכן החוק נאכף: מול ה-Repository, שהוא הגבול היחיד שהמודולים רואים, ולהטיל על הדרייבר את החובה המשלימה, לא לאבד שורה. הקוד כבר עושה ג: הבדיקות האדומות והבדיקה של BL-01 עוברות על שני הדרייברים, ודרייבר הענן נבדק על כשל רשת ועל דחייה. **ההמלצה: ג.** המחיר, לרישום: בין הכתיבה בזיכרון לבין קבלתה במסד יש חלון שבו המכשיר עלול לכבות, והשורה תאבד עם הזיכרון. הכותרת מראה "ממתין לשליחה" בחלון הזה, וריקון התור נעשה אחרי כל תשובה ובסיום סשן.

**פער 63, שלוש הדרכים**: (א) להשאיר עמוד אחד עם כל המסכים: אינו מתאים למטייל ולא למבחן שלוש המשפחות, ואינו קובע מי רואה מה. (ב) כתובת נפרדת לכל מסך, כלומר ארבעה קובצי כניסה: מכפיל את ההרכבה פי ארבעה, ומבחן מבנה 03 ("כל מסך פונה לכתובת אחת") נבדק בארבעה מקומות. (ג) נקודת כניסה אחת שבוחרת מסך לפי פרמטר, ובלי פרמטר מציגה הכול לפיתוח: ההרכבה נשארת אחת, המסכים אינם משתנים, וההבדל בין מטייל לחוקר הוא קישור. **ההמלצה: ג.** מה שדרך ג אינה נותנת, ובכוונה: היא אינה אימות זהות. מי שמחזיק את הקישור לפאנל רואה את הפאנל, כפי שהיה עד היום, ומפה 2.1 (ADMIN_USERS) קובעת שסיסמה נכנסת לפני שהפאנל יוצא למכון. זו הכרעת אפיון, ולכן היא מוצעת ואינה נכתבת כנוסח סופי.

## 3. מפת המודולים: גרסה 3.10 המוצעת

### 3.1 סעיף 2.1, פסקה חדשה אחרי טבלת הישויות (פער 60)

> **המפתח הראשי** (פער 60, הוכרע 16.09.2026): שדה המזהה של כל ישות, הראשון ברשימת השדות (item_id, site_id, anchor_id, source_id, institute_id, mou_id, approval_id, user_id, session_id, exit_id, interaction_id), הוא המפתח הראשי שלה בכל מקום אחסון. בטבלאות המערכת: audit_log לפי request_id ו-phase יחד, reference לפי המפתח. אין שדה מזהה נוסף, ואין מזהה שמומצא באחסון.

### 3.2 סעיף 2.3, שורת BL-01 (פער 58)

**היום**: "אין שינוי מצב של פריט או מסלול בלי רשומת APPROVALS. כשל כתיבה מבטל את המעבר"

**מוצע**: "אין שינוי מצב של פריט או מסלול בלי רשומת APPROVALS. כשל כתיבה מבטל את המעבר. הכשל נמדד מול ה-Repository, הגבול היחיד שהמודול רואה (פער 58, הוכרע 16.09.2026): דרייבר שמחזיק תור כתיבה מקבל את השורה בזיכרון ומתחייב שלא לאבד אותה עד שהאחסון קיבל אותה, ודחייה קבועה נרשמת ומדווחת"

### 3.3 סעיף 2.3, שורת BL-10 (פער 58)

**היום**: "יומן ההחלטות (APPROVALS) חוסם פעולה כשאינו נכתב; יומן האינטראקציות (INTERACTIONS) לעולם אינו חוסם את החוויה בשטח, מנסה שוב ומסמן partial_log"

**מוצע**: "יומן ההחלטות (APPROVALS) חוסם פעולה כשאינו נכתב; יומן האינטראקציות (INTERACTIONS) לעולם אינו חוסם את החוויה בשטח, מנסה שוב ומסמן partial_log. 'נכתב' פירושו התקבל ב-Repository (פער 58); הדרך מה-Repository אל האחסון היא של הדרייבר, לפי BL-01"

### 3.4 סעיף 4.3, פסקה חדשה (פער 63, רק אם תאשרי)

> **בחירת המסך בהרכבה** (פער 63, להכרעה): נקודת הכניסה אחת, והיא מציגה מסך אחד לפי פרמטר screen בכתובת, שערכו הוא שם הפונה של המסך (screen-traveler, screen-veto, screen-content, screen-owner). בלי פרמטר היא מציגה את הרכבת הפיתוח, ארבעת המסכים יחד. הפרמטר בוחר מה מוצג ואינו מרשה דבר: מה כל מסך רשאי לבקש נקבע ברשימת המותר בלבד, כמו היום. אימות זהות אינו כאן: הוא נכנס לפי 2.1, שורת ADMIN_USERS.

### 3.5 סעיף 6.4, מבחן 01 (פער 59)

**היום**: "01 חיבור לנתונים בקובץ אחד"

**מוצע**: "01 חיבור לנתונים בקובץ אחד לכל דרייבר: אחסון הדפדפן בקובץ אחד, מחרוזת החיבור לענן בקובץ אחד, ואף קובץ מחוץ ל-CORE-04 אינו נוגע באף אחד מהם (פער 59, 16.09.2026)"

### 3.6 סעיף 7, שורת שלב 5

**היום**: "... חלק א נמסר 15.09.2026; חלק ב, דרייבר הענן והכתובת לטלפון, אחריו"

**מוצע**: "... חלק א נמסר 15.09.2026; חלק ב נמסר ומוזג 16.09.2026: דרייבר הענן על Supabase, והכתובת לטלפון ב-GitHub Pages (פער 61, decision-05 גרסה 1.1)"

### 3.7 שורת היומן של הגרסה החדשה

> גרסה 3.10, 16.09.2026: ששת הפערים שדוח שלב 5 חלק ב החזיר, בהכרעת בעלת הפרויקט. 2.1: שדה המזהה של כל ישות הוא המפתח הראשי, בלי שדה חדש (פער 60). 2.3: BL-01 ו-BL-10 קיבלו משפט הבהרה, הכשל נמדד מול ה-Repository והדרייבר מתחייב שלא לאבד שורה (פער 58). 4.3: בחירת המסך בהרכבה לפי פרמטר בכתובת (פער 63) [נכנס רק אם אושר]. 6.4: מבחן 01 מנוסח לשני דרייברים (פער 59). 7: חלק ב של שלב 5 נמסר, והכתובת לטלפון ב-GitHub Pages (פער 61). **הליבה לא נגעה**: המעטפות, חמשת הצעדים, 23 קודי השגיאה, 27 שורות 4.2, 49 שורות רשימת המותר, 11 הישויות ושדותיהן, ו-BL-01 עד BL-21 כמספרם. שום דבר שאושר בגרסה 3.9 לא בוטל.

## 4. decision-05: גרסה 1.1 המוצעת (פער 61)

**סעיף 1, הטבלה, שורת "הכתובת לטלפון"**, היום: "דלי אחסון ציבורי שמגיש קובץ בכתובת HTTPS | הקובץ הארוז מועלה לדלי; GPS, מיקרופון וקול דורשים HTTPS (PRD סעיף 29)"

**מוצע**: "אינו מכסה. הדלי הציבורי של הספק מחזיר HTML כטקסט לפי התיעוד שלו, ופונקציית קצה מוגשת בארגז חול שחוסם סקריפטים (פער 61, 16.09.2026) | הכתובת לטלפון היא GitHub Pages של המאגר, שהפך לציבורי בהכרעת בעלת הפרויקט: תהליך Actions אורז את הקובץ עם שני משתני הסביבה מסודות המאגר ומעלה אותו. הערכים אינם ב-Git. GPS, מיקרופון וקול דורשים HTTPS (PRD סעיף 29), ו-Pages נותן אותו"

**סעיף 5, שורת "חלק ב של שלב 5"**: נסגר. **מוצע**: "חלק ב של שלב 5 נמסר ומוזג 16.09.2026. הדלי app והפונקציה app שנוצרו בניסיון האירוח נמחקים מהפרויקט".

**סעיף 6, מה אינו משתנה**, תוספת: "המסד, הדרייבר והאריזה אינם תלויים במארח: הקובץ הארוז פונה למסד לפי הכתובת שבבלוק ההגדרות מכל כתובת שממנה הוא נפתח. החלפת המארח היא העלאת אותו קובץ למקום אחר."

שורת סטטוס: "גרסה 1.1, 16.09.2026: שורת הכתובת לטלפון תוקנה לפי פער 61. ההכרעה על הספק למסד לא השתנתה."

## 5. מסמך הבנייה: גרסה 2.9 המוצעת

1. **סעיף 1, שורת מסמך הייחוס**: "doc-module-map-v3.md (גרסה 3.10, מאושרת 16.09.2026)". בדיקת המבנה document-versions דורשת זאת.
2. **סעיף 3, שורת "יעד ההרצה, שלבים 5 עד 7"**, היום: "ענן (decision-04 ב4). הספק: Supabase (decision-05, 16.09.2026): מסד Postgres דרך ממשק ה-REST שלו, והקובץ הארוז מוגש מדלי אחסון ציבורי בכתובת HTTPS". **מוצע**: "ענן (decision-04 ב4). המסד: Supabase (decision-05, 16.09.2026), Postgres דרך ממשק ה-REST שלו. הקובץ הארוז: GitHub Pages של המאגר (פער 61), בתהליך Actions שאורז אותו עם משתני הסביבה מסודות המאגר בכל דחיפה ל-main".
3. **סעיף 5, עץ הקבצים** (פער 62), שתי שורות חדשות:

```
/repository/migrations/   העתקי המיגרציות שהוחלו על מסד הענן, לקריאה. אינם קוד המערכת
/.github/workflows/       הגדרות הפריסה: pages.yml אורז ומעלה ל-Pages. אינו קוד המערכת ואינו נסרק במבחני המבנה
```

4. **סעיף 5, שורת /index.html** (פער 63, רק אם תאשרי), היום: "נקודת הכניסה: קוראת את הנתונים, בונה דרייבר, Repository ו-Orchestrator, ומגישה את המסכים". **מוצע**: "נקודת הכניסה: קוראת את הנתונים, בונה דרייבר, Repository ו-Orchestrator, ומגישה מסך אחד לפי הפרמטר screen בכתובת, או את כולם בלי פרמטר (מפה 4.3)".

שורת היומן: "גרסה 2.9, 16.09.2026: המפה עלתה ל-3.10 ושורת מסמך הייחוס עודכנה אליה. סעיף 3: הכתובת לטלפון ב-GitHub Pages ולא בדלי הספק (פער 61, decision-05 גרסה 1.1). סעיף 5: נוספו /repository/migrations/ ו-/.github/workflows/ כהגדרות ולא כקוד (פער 62) [ושורת index.html, אם פער 63 אושר]. שום דבר שאושר בגרסה 2.8 לא בוטל."

## 6. שינוי הקוד שנגזר מהתיקון

אם פער 63 מאושר: נקודת הכניסה קוראת את הפרמטר screen ומרכיבה מסך אחד או את כולם. הסימולטור ופאנל הפיתוח מוצגים רק בהרכבת הפיתוח. בדיקת המבנה של נקודת הכניסה מקבלת טענה על הפרמטר. אף מסך, מודול או שורה ברשימת המותר אינם משתנים. מבוצע על ענף השלב שבו יוכרע, עם Regression מלאה.

בלי אישור פער 63: אין שינוי קוד. חמשת הפערים האחרים מתארים מה שכבר נבנה ונכתב.

## 7. מה במפורש לא השתנה

- לא נוסף ולא הוסר אף מודול: 22, כמו בגרסה 3.9.
- לא נוספה ולא הוסרה אף פעולה: 27 שורות ב-4.2, 49 שורות ברשימת המותר.
- לא נוסף ולא הוסר אף קוד שגיאה: 23.
- אף ישות לא קיבלה שדה: פער 60 קובע מפתח משדה קיים.
- אף חוק עסקי לא נמחק ולא נוסף. BL-01 ו-BL-10 קיבלו משפט הבהרה, ולא כלל חדש.
- ההכרעה על הספק למסד (decision-05 סעיף 1, שורת המסד) לא השתנתה. השתנתה רק שורת הכתובת לטלפון.
- B-36 (תיקון 1 של F-13 אינו במאגר) ופערים 1 עד 57 לא נגעתי בהם.

## 8. הצעד הבא

אישור הגרסאות (מפה 3.10, מסמך בנייה 2.9, decision-05 גרסה 1.1) בידך, ומילה נפרדת על פער 63: מאושר, נדחה לשלב 6 או 7, או דרך אחרת. אחרי האישור: הגרסאות נכנסות למאגר בקומיט אחד, הדלי והפונקציה נמחקים מהפרויקט בענן, ההעתקה ל-prd-update-inbox לפי פרוטוקול הכתיבה, ואם 63 אושר הוא נבנה כמשימה אחת עם Regression. האימות בטלפון (14.2) נשאר פתוח לדיווחך ואינו תלוי בצעד הזה.
