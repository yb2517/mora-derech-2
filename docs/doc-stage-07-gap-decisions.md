# תיקון המסמכים מפערי שלב 7

סטטוס: אושר 18.09.2026 בידי בעלת הפרויקט. הגרסאות הוחלו במאגר: מפה 3.11, מסמך הבנייה 2.10. פרומפט 4 של מנגנון הבנייה, ריצה חמישית בפרויקט.
תאריך: 17.09.2026
המקור: doc-stage-07-plan.md סעיף 5 (הפערים 65 עד 68, שהכרעתם המוצעת אושרה עם התוכנית) ורישום הפערים סעיף 5א.
חוק ברזל של הצעד: מתקנים רק את מה שעולה מהפערים. לא נוספו מודולים, פעולות או יכולות שאיש לא ביקש. פער שמערער על הליבה או על גבולות ה-MLP חוזר אלייך כהחלטת אפיון. אף אחד מארבעת הפערים אינו כזה: שניים משלימים את המסמכים אל מה שהקוד כבר עושה, אחד הוא שורת ציוד, ואחד הוא ניסוח בדיקת קבלה.

## 1. ההכרעות, בשורה אחת לכל פער

| # | הפער | ההכרעה שאושרה עם התוכנית | המסמך והסעיף שמשתנה |
|---|---|---|---|
| 65 | אין פעולה ב-4.2 שיוצרת רשומת SITES, ואין במפה אמירה כיצד מסלול נכנס למערכת | המסלול הוא נתון שנזרע מחוץ לזמן ריצה, כמו מפתחות ה-reference (BL-09). כלי הייבוא כותב אותו דרך הדרייבר. אין פעולה חדשה ואין שורה ברשימת המותר: מסלול שני הוא רשומה, לא קוד | מפה 2.1, פסקה חדשה אחרי פסקת "גבולות המסלול" |
| 66 | אימות עוגן בשטח דורש את מיקום המכשיר במסך התוכן, ומפה 4.1 מזריקה את CONN-03 ל-AUTO-01 בלבד | CONN-03 מוזרק גם למסך הניהול, כאותו ציוד, בהרכבה. המסך ממשיך לשלוח מעטפות בלבד; מבחני מבנה 03 ו-08 אינם משתנים | מפה 4.1, משפט בפסקת שיוך המתאמים |
| 67 | /data/corpus/ אינה בעץ הקבצים של מסמך הבנייה סעיף 5; /data/demo/ ו-/tools/seed-cloud.js יצאו מהמאגר | שורה ל-/data/corpus/ במקום שורת /data/demo/; שורת /tools/ מקבלת את כלי הייבוא לצד הסימולטור; סעיף 7 עובר ללשון עבר | מסמך הבנייה סעיף 5 ו-7 |
| 68 | בדיקת הקבלה של שלב 7 תלויה ב-L4, כלומר בהסכם שה-PRD מונה כפער חוסם, ואף מסמך אינו אומר אם השלב נסגר כשהחתימה ממתינה | השלב נסגר כשהמערך מוכיח את הנעילה על 19 הטקסטים והשטח אומת; הנעילה החיה נרשמת בדוח כממתינה להסכם או כבוצעה | מפה 7, שורת שלב 7; מסמך הבנייה סעיף 6, שורת שלב 7 |

## 2. מה הקוד כבר עושה, ומה ממתין לאישור

הפערים 65, 67 ו-68 מתארים מצב שכבר נבנה או נקבע: כלי הייבוא זורע את רשומת המסלול דרך הדרייבר (משימה 4, ירוק בבדיקה), הקורפוס יושב ב-/data/corpus/ (משימה 3), ובדיקת ה-System של משימה 9 מוכיחה את הנעילה על 19 הטקסטים. פער 66 בלבד גוזר קוד שטרם נכתב: משימה 7 בתוכנית, ההזרקה של CONN-03 למסך הניהול ולחצן "מיקום המכשיר". היא ממתינה לאישור הנוסח כאן, לפי הכלל "המפה משתנה לפני הקוד".

## 3. מפת המודולים: גרסה 3.11 המוצעת

### 3.1 סעיף 2.1, פסקה חדשה אחרי "גבולות המסלול" (פער 65)

> **רשומת המסלול** (פער 65, הוכרע 17.09.2026): אין ב-4.2 פעולה שיוצרת מסלול. המסלול הוא נתון שנזרע מחוץ לזמן ריצה, כמו מפתחות ה-reference (BL-09), בידי בעלת הפרויקט או בכלי ייבוא שכותב דרך הדרייבר; מרגע שהוא קיים, כל שינוי במצבו הוא פעולה של BE-05 לפי 2.1. מסלול שני הוא רשומה נוספת, לא קוד, ולא שורה ברשימת המותר.

### 3.2 סעיף 4.1, פסקת שיוך המתאמים (פער 66)

**היום**: "CONN-03 מוזרק ל-AUTO-01, ו-TOOL-01 הוא מקור דגימות חלופי שמוזרק במקומו בפיתוח (שורותיו ברשימת המותר נוגעות ל-arrive ול-leave בלבד, ואינן נוגעות לדגימות)."

**מוצע**: "CONN-03 מוזרק ל-AUTO-01, ו-TOOL-01 הוא מקור דגימות חלופי שמוזרק במקומו בפיתוח (שורותיו ברשימת המותר נוגעות ל-arrive ול-leave בלבד, ואינן נוגעות לדגימות). CONN-03 מוזרק גם למסך הניהול, כאותו ציוד (פער 66, הוכרע 17.09.2026): אימות עוגן בשטח (usecase-f-08 צעד 7) משווה את הקואורדינטות הרשומות למיקום המכשיר, והמסך מקבל את הדגימה מהמתאם וממשיך לשלוח מעטפות בלבד."

### 3.3 סעיף 7, שורת שלב 7 (פער 68)

**היום**: "| 7 | ניקוי נתוני הדגמה, הסרת פעולת האיפוס, הקורפוס לרשומות, אימות עוגנים, is_crossing ונקודות יציאה בשטח | L3 עובר על 19 הפריטים; נעילה ראשונה | נקודה שהיא גם צומת וגם תוכן (הכרעה פתוחה 4) |"

**מוצע**: "| 7 | ניקוי נתוני הדגמה, הסרת פעולת האיפוס, הקורפוס לרשומות, אימות עוגנים, is_crossing ונקודות יציאה בשטח | L1 עד L4 עוברים על 19 הפריטים ונעילה ראשונה נרשמת, במערך הבדיקות על הטקסטים האמיתיים; הנעילה החיה היא פעולת אדם אחרי רישום ההסכם, והדוח מציין אם בוצעה או ממתינה (פער 68, הוכרע 17.09.2026) | נקודה שהיא גם צומת וגם תוכן (הכרעה פתוחה 4) |"

### 3.4 סעיף 3.3, שורת TOOL-01 ושורת GW-01: ללא שינוי. סעיף 10, שורת יומן

> גרסה 3.11, 17.09.2026: ארבעת הפערים שתוכנית שלב 7 חשפה, בהכרעת בעלת הפרויקט. 2.1: רשומת המסלול היא נתון שנזרע מחוץ לזמן ריצה (פער 65). 4.1: CONN-03 מוזרק גם למסך הניהול לאימות השטח (פער 66). 7: בדיקת הקבלה של שלב 7 מבחינה בין הנעילה במערך לנעילה החיה (פער 68). **הליבה לא נגעה**: המעטפות, חמשת הצעדים, 23 קודי השגיאה, 27 שורות 4.2, 46 שורות רשימת המותר בייצור, 11 הישויות ושדותיהן, ו-BL-01 עד BL-21 כמספרם. שום דבר שאושר בגרסה 3.10 לא בוטל.

## 4. מסמך הבנייה: גרסה 2.10 המוצעת

### 4.1 שורת הסטטוס וסעיף 1

הסטטוס: "גרסה 2.10, מאושרת 17.09.2026". סעיף 1, שורת מסמך הייחוס: "doc-module-map-v3.md (גרסה 3.11, מאושרת 17.09.2026)". בדיקת המבנה document-versions אוכפת את השתיים יחד.

### 4.2 סעיף 5, עץ הקבצים (פער 67)

**היום**: "/data/demo/               נתוני הדגמה מסומנים" ו-"/tools/simulator.js       TOOL-01, פיתוח בלבד"

**מוצע**: השורה /data/demo/ יורדת, ובמקומה:

> /data/corpus/             הקורפוס כנתונים: מקור הייבוא ועותק לקריאה. אינו נקרא בזמן ריצה ואינו נארז

ושורת /tools/ מתפצלת:

> /tools/simulator.js       TOOL-01, פיתוח בלבד
> /tools/import-corpus.js   כלי הייבוא של הקורפוס, פיתוח בלבד. מרכיב כמו נקודת הכניסה ושולח מעטפות
> /tools/bundle.js          סקריפט האריזה. אינו קוד המערכת

### 4.3 סעיף 6, שורת שלב 7 (פער 68)

**היום**: "L1 עד L4 עוברים על 19 הפריטים; נעילה ראשונה מצליחה ונרשמת"

**מוצע**: "L1 עד L4 עוברים על 19 הפריטים ונעילה ראשונה מצליחה ונרשמת, במערך הבדיקות על הטקסטים האמיתיים ובהסכם סינתטי של הבדיקה. במסד החי: 19 הפריטים draft אחרי הייבוא, שאילתה על is_demo מחזירה אפס, והנעילה היא פעולת אדם אחרי רישום ההסכם; הדוח מציין אם בוצעה או ממתינה (פער 68)"

### 4.4 סעיף 7, נתוני ההדגמה (פער 67)

**מוצע**, במקום הסעיף כולו:

> ישבו ב-/data/demo/ ומסומנו בשדה is_demo עד שלב 7. הוסרו במשימות 2 ו-5 של שלב 7 (17.09.2026): מהמאגר ומהמסד החי, בעשר טבלאות הישויות. בדיקת הקבלה: שאילתה על is_demo מחזירה אפס. הזריעה הסינתטית של הבדיקות יושבת מאז ב-/tests/helpers/fixtures.js, ואינה נתוני מערכת. העמודה is_demo נשארת בסכימה עד להכרעה נפרדת, מפני שהסרתה היא שינוי Schema.

### 4.5 סעיף 13, שורת יומן

> גרסה 2.10, 17.09.2026: המפה עלתה ל-3.11 ושורת מסמך הייחוס עודכנה אליה. סעיף 5: /data/corpus/ במקום /data/demo/, וכלי הייבוא וסקריפט האריזה ב-/tools/ (פער 67). סעיף 6, שורת שלב 7: ההבחנה בין הנעילה במערך לנעילה החיה (פער 68). סעיף 7: נתוני ההדגמה הוסרו, בלשון עבר (פער 67). שום דבר שאושר בגרסה 2.9 לא בוטל.

## 5. שינוי הקוד שנגזר מהתיקון

משימה 7 בתוכנית, אחרי אישור 3.2: ההרכבה בנקודת הכניסה מזריקה את CONN-03 למסך הניהול; בטופס העוגן לחצן "מיקום המכשיר" שממלא lat ו-lng מהדגימה האחרונה ומציג את המרחק מהקואורדינטות הרשומות; בלי מתאם מוזרק הלחצן אינו מוצג. בדיקת ה-System של מסך הניהול מקבלת את המקרה. אין קוד אחר שנגזר.

## 6. מה במפורש לא השתנה

הליבה: המעטפות, חמשת הצעדים, 23 קודי השגיאה, 27 שורות 4.2, רשימת המותר, 11 הישויות ושדותיהן, BL-01 עד BL-21. חוקי הברזל ומבחני המבנה. ההכרעות הפתוחות 1, 3, 4, 6 עד 11 ו-13. העמודה is_demo בסכימה. הכרעה 4 של התוכנית (הפריטים נכנסים כ-draft, ההכרעה היא של אדם) והכרעה 8 (אין הסכם סינתטי במסד החי).

## 7. הצעד הבא

מילת אישור על הנוסחים. אחריה: המפה 3.11 ומסמך הבנייה 2.10 מוחלים במאגר בקומיט מסומן, העתקים ל-prd-update-inbox לפי הסקיל, משימה 7 נכתבת, ואז Regression מלאה ודוח השלב.
